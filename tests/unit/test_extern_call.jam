extern fn abs(x: i32) i32;

fn callAbsNeg() i32 {
    return abs(-7);
}

fn callAbsPos() i32 {
    return abs(42);
}

tfn callAbsNeg() {
    assert(callAbsNeg(), 7);
}

tfn callAbsPos() {
    assert(callAbsPos(), 42);
}

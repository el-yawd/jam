.PHONY: run install clean

# Default target: serve the site locally with live reload.
run:
	bundle exec jekyll serve --livereload

# Install Ruby gems listed in Gemfile.
install:
	bundle install

# Wipe the generated _site directory.
clean:
	rm -rf _site
